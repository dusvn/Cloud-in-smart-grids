# Cloud-in-smart-grids

Testing commands type in Command Prompt(run as admin) to simulate fault state of server:

-netstat -ano | findstr : ENTER_PORT

taskkill /F /PID ENTER_PID or if there is more instances of processes (taskkill /F /PID ENTER_PID) && (taskkill /F /PID ENTER_PID)&& (taskkill /F /PID ENTER_PID)

